# Dynamic-Variable-Diagrams
Diagram Maker that gives the ability to be able to set variables and relationships between different nodes, having it dynamically update

# How to run

```
cd dynamic-variable-diagrams
npm install
npm run
```